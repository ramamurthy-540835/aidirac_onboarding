import { requireUserWorkspace, userWorkspaceError } from "@/lib/user-workspace/auth";
import { adapterTodo, markNotificationRead } from "@/lib/user-workspace/mutations";
export async function POST(request: Request, context: { params: Promise<{ notificationId: string }> }) { try { await requireUserWorkspace(request); const item = markNotificationRead((await context.params).notificationId); return item ? Response.json({ data: item, source: "adapter", todo: adapterTodo }) : Response.json({ error: "Notification not found" }, { status: 404 }); } catch (error) { return userWorkspaceError(error); } }
